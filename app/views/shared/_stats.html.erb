<% @user ||= current_user %>
<div class="stats">
  <%= link_to relationships_user_path(@user, relationship: t("views.following").downcase) do %>
    <strong id="following" class="stat">
      <%= @user.following.count %>
    </strong>
    <%= t ("views.following") %>
  <% end %>
  <%= link_to relationships_user_path(@user, relationship: t("views.unfollowing").downcase )do %>
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    <%= t ("views.unfollowing") %>
  <% end %>
</div>